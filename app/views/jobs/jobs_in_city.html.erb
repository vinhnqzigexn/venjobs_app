<h4><%= @city.name %>: <%= pluralize(@city.jobs.count, 'job') %></h4>
<div class='container'>
  <div class='jobs-list'>
    <ul>
      <%= paginate @jobs, outer_window: 3 %>
      <%= render @jobs %>
    </ul>
  </div>
</div>